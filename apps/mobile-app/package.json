{
  "name": "@cup/mobile-app",
  "private": true,
  "scripts": {
    "build": "turbo run turbo:build && pnpm run native:sync",
    "native:sync": "CUP_NATIVE_PLATFORM=android cap sync android && CUP_NATIVE_PLATFORM=ios cap sync ios",
    "turbo:build": "tsc --build --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1",
    "@cup/web-app": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1",
    "@patricktree-stack/config-typescript": "workspace:*",
    "@types/node": "catalog:",
    "@typescript/native": "catalog:",
    "typescript": "catalog:"
  }
}
