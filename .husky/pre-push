while read -r local_ref local_oid remote_ref remote_oid; do
    if [ "${remote_ref}" = "refs/heads/main" ] && [ "${local_ref}" != "(delete)" ]; then
        pnpm run validate:extended
        exit $?
    fi
done
